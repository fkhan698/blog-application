<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/public/stylesheets/index.css">
    <title>Home Page</title>
</head>
<body>
    <h1 class="home-page"> Blog Posts <h1>
        <div class="new-articles">
                <button class="new-article-button" onclick="window.location.href='/articles/new'">New Article</button>
                <form action="/logout?_method=DELETE" method="POST">
                <button class="logout-button" type="submit">Log Out</button>
                </form>
        </div>
        <div class="container">
            <div class="articles">
                <% articles.forEach(article => { %>
                    <div class="article-card">
                            <h1 class="article-title"><%= article.title %></h1>
                            <p class="article-description"> <%= article.description %></p>
                            <p class="article-date"> <%= article.createdAt.toLocaleDateString('en-US')%></p>
                            <div class="article-buttons">
                                
                                <form action="/articles/<%= article.id %>?_method=DELETE" method="POST">
                                    <button class="delete-button">Delete</button>
                                </form>
                                <button class="read-more-button" onclick="window.location.href='articles/<%= article.slug %>'">Read More</button>
                                <button class="edit-button" onclick="window.location.href='articles/edit/<%= article.slug %>?_method=PUT'">Edit</button>
                            </div>
                    </div>

                <% })%>
            </div>
        
        
            
        </div>
</body>
</html>